# Chave da API Anthropic — usada para ler a foto da nota fiscal.
# Sem ela, o app continua funcionando em modo lançamento manual.
ANTHROPIC_API_KEY=

# Onde o SQLite fica. Padrão: ./.data
# COFRIN_DATA_DIR=/caminho/para/dados
